---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: gateways.foomo.org
spec:
  group: foomo.org
  names:
    kind: Gateway
    listKind: GatewayList
    plural: gateways
    shortNames:
    - gw
    singular: gateway
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Gateway represents a service registration in the gateway.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of a Gateway resource.
            properties:
              addToRobots:
                type: string
              expose:
                items:
                  description: Expose defines an exposed path configuration.
                  properties:
                    cmsApp:
                      description: Named application in cms
                      type: string
                    cmsMimetypes:
                      description: Contentserver mimetypes
                      items:
                        description: MimeType represents a MIME type.
                        type: string
                      type: array
                    description:
                      description: A description of the exposition, to be show in
                        gateway api
                      type: string
                    internalAccessGroups:
                      description: Access is only granted to these internal groups
                      items:
                        description: InternalAccessGroup represents an internal access
                          group identifier.
                        type: string
                      type: array
                    paths:
                      description: paths in urls to register for, lookup is automatically
                        long to short
                      items:
                        description: Path represents a URI path.
                        type: string
                      type: array
                    stripBasePath:
                      description: Service is exposed at /foo request is /foo/bar,
                        stripBasePath == true => requests will go to /bar and /foo
                        will be set as x-base-path header
                      type: boolean
                  type: object
                type: array
              service:
                description: Service represents a backend service identifier.
                minLength: 1
                type: string
              sitemap:
                type: string
            required:
            - service
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
